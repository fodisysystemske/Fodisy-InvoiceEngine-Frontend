{
  "version": 2,
  "builds": [
    { "src": "dist/enterprise-invoice-frontend/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*\\.(js|css|map|ico|png|json))$", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}